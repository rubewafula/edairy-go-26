APP_NAME={{APP_NAME}}
APP_ENV=production
PORT={{API_PORT}}
DB_HOST=host.docker.internal
DB_PORT=3306
DB_NAME={{DB_NAME}}
DB_USER=edairy
DB_PASSWORD={{DB_PASSWORD}}
ENV=production
JWT_SECRET={{JWT_SECRET}}
JWT_EXPIRY_HOURS=24
GIN_MODE=release
